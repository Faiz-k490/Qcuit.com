"""api package initialization (keeps api as a proper package)."""

# Intentionally empty - marks this directory as a Python package so
# relative imports (from . import gates) work when running as a module.
