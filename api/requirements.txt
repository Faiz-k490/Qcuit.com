# Python dependencies for the Vercel Serverless backend.
# Runtime subset of website/requirements.txt (no gunicorn / pytest — Vercel
# provides the server and tests don't run in the function bundle).
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
flask-cors
Werkzeug==3.1.6
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.3.0
blinker==1.9.0
numpy==2.3.4

# Postgres driver (auth/blog/user routes when DATABASE_URL is set)
psycopg2-binary==2.9.12

# Qiskit exports are generated as source strings; the serverless API does not
# execute Qiskit/Aer at runtime.
